{
  "version": 2,
  "buildCommand": "ls -d */ && cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "cleanUrls": true,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "VITE_API_URL": "https://beyondchats-backend-p8s9.onrender.com/api/articles"
  }
}